% original program
proc exprs(a,b,c,x,y,z) is {
    x := 1+2-3*x--y;  % 1
    a := b/(b-c);     % 2
    if (x > y and a < b) % 3 
    then
        z := (2*(a*b)+c*c)-4*a*c % 4
    else {
        while (x < 0 or -y > 3 and b <= c and c >= b) % 5
        do z := x*x*x*(-y) % 6
    }
}