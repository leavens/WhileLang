program cfg1(q,r,x,y) is {
    q := 0; % 1
    r := x; % 2
    while (r >= y) do % 3
    {
        r := (r - y); % 4
        skip; % 5
        q := (q + 1)    %6
    }
    % ; assert r < y and q*y+r == x  %7
}